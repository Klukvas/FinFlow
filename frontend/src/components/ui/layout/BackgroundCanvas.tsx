import React from 'react';

/**
 * BackgroundCanvas — disabled.
 * Previously rendered animated particle grid. Now returns null for clean SaaS aesthetic.
 */
export const BackgroundCanvas: React.FC = () => {
 return null;
};
